class Category

types
	public Name = seq of (char);
	
instance variables
  public name: Name;
  
operations
 public Category : Name ==> Category
	Category(nm) == (
	name := nm;
  return self
	);
end Category
